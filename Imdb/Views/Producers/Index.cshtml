@model IEnumerable<Imdb.Models.Producer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Person.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.Sex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.Dob)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.Bio)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person.Name)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Person.Sex)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.Person.Dob)
        </td>

        <td>
            @Html.DisplayFor(modelItem => item.Person.Bio)
        </td>

        
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
